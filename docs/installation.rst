.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install ldap_target_ctl
    $ easy_install ldap_target_ctl

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ldap_target_ctl
    $ pip install ldap_target_ctl
